import { StyleSheet } from 'react-native';
import { Button } from 'native-base';

export default StyleSheet.create({
    container: {
        flex: 1,
        flexDirection: `row`,
        flexWrap: `wrap`
    },

    Button: {
        margin: 18,
        width: 100
    }

})